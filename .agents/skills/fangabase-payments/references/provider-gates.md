# Portes fournisseur

Exiger documentation officielle, sandbox, secrets s?par?s test/live, signature v?rifiable, mapping des statuts et tests contractuels. Si une API publique ne suffit pas : `NEEDS_PROVIDER_CONTRACT`. Les mocks prouvent le contrat local, jamais la production. Stripe et FedaPay restent `IMPLEMENTED_NEEDS_SANDBOX_UAT` jusqu?au passage sandbox avec compte r?el.
