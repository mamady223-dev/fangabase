version: 1
product:
  name: Exemple Orange Money Mali
  slug: exemple-orange-money-mali
  type: commerce
  description: Profil headless avec intégration Orange Money Mali facultative
  locale: fr
  timezone: Africa/Bamako
  country: ML
  default_currency: XOF
architecture: { target: cloud_vercel, frontend: next, backend: next, ui: next }
database: { engine: postgres, provider: neon }
email: { provider: local_log }
storage: { provider: local_private }
queue: { provider: database }
cache: { provider: memory_dev }
billing: { modes: [one_time] }
payments:
  providers: [orange_money_ml]
  default_provider: orange_money_ml
design: { source: headless }
features:
  organizations: true
  marketplace: false
  admin: true
  audit_log: true
  notifications: true
  uploads: true
