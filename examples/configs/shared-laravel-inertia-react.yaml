version: 1
product:
  name: Shared Laravel Inertia React
  slug: shared-laravel-inertia-react
  type: saas
  description: Profil intégré Laravel React Inertia pour hébergement mutualisé
  locale: fr
  timezone: Africa/Bamako
  country: ML
  default_currency: XOF
architecture:
  target: shared_laravel
  frontend: react
  backend: laravel
  ui: inertia_react
  integration: inertia
deployment:
  family: shared
  docker: false
  database: mysql
  vps_variant: null
database: { engine: mysql, provider: mysql }
email: { provider: local_log }
storage: { provider: local_private }
queue: { provider: database }
cache: { provider: database }
billing: { modes: [credits, subscription] }
payments: { providers: [], default_provider: null }
design: { source: headless }
features:
  organizations: true
  marketplace: false
  admin: true
  audit_log: true
  notifications: true
  uploads: true
