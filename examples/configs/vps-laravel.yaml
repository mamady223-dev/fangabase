version: 1
product:
  {
    name: VPS Laravel,
    slug: vps-laravel,
    type: saas,
    description: Profil CI FangaBase,
    locale: fr,
    timezone: Africa/Bamako,
    country: ML,
    default_currency: XOF,
  }
architecture:
  { target: vps_laravel, frontend: blade, backend: laravel, ui: blade }
database: { engine: postgres, provider: postgres }
email: { provider: smtp }
storage: { provider: local_private }
queue: { provider: redis }
cache: { provider: redis }
billing: { modes: [credits, subscription] }
payments:
  providers: [fedapay]
  default_provider: fedapay
design: { source: headless }
features:
  {
    organizations: true,
    marketplace: false,
    admin: true,
    audit_log: true,
    notifications: true,
    uploads: true,
  }
