# Les architectures FangaBase

Cloud Vercel convient au d?ploiement g?r? Next.js/PostgreSQL. VPS Next.js garde une seule pile TypeScript avec worker permanent. VPS Laravel privil?gie PHP et queues. Le mutualis? retire toute d?pendance ? un processus Node permanent. L?hybride associe Next.js global ? Laravel comme autorit? m?tier.

Le choix ne change jamais les contrats, les codes d?erreur, les r?gles financi?res ou la s?curit?. Voir les templates dans `templates/` pour les commandes, sauvegardes et rollbacks.
