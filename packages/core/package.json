{
  "name": "@fangabase/core",
  "version": "0.2.0-rc.2",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:integration": "vitest run src/integration.test.ts"
  },
  "dependencies": {
    "@fangabase/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.15.32"
  }
}
