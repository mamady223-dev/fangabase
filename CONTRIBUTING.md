# Contribuer

Cr?ez une branche courte, ajoutez un test pour chaque changement et ex?cutez `pnpm release:check` ainsi que `composer --working-dir=apps/server test`. Ne placez aucun secret dans un commit. Une modification financi?re exige un cas de concurrence ou d?idempotence adapt?.
